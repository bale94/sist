1. 외부 객체 생성자 멤버(구성요소)관련 에러 발생
	==>
	클래스를 만들고 반드시 저장을해야 다른파일에서 객체로 가져다 쓸 수 있으므로 주의할것
	
# frontend단 에러 처리(js에러)
0. 웹서버 console에는 에러가 없고, 
1. 이벤트 작동(click, submit등)이 되지 않을때
2. f12를 눌러 해당 에러 라인을 찾는다
	- 해당라인의 앞, 뒤라인 모두 체크
		if(조건){ : 중괄호, 소괄호 블록을 빠트린 경우
		==> 습관적으로 중괄호 소괄호를 시작과 닫기를 먼저하고 진행
		if(){
		
		}
# backend단 에러
1. console 창에서 확인 - db에러 mybatis sqlSession ==> mapper관련 에러
2. property 처리 값 처리 getXXX(), setXXX()
		
		
		
		
		
		
		
		
		
		
		